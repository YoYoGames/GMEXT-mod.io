
if (os_type != os_xboxone && 
	os_type != os_xboxseriesxs && 
	os_type != os_switch && 
	os_type != os_ps5 && 
	os_type != os_ps5) {
	instance_destroy();
	exit;
}


depth = -100

pressed = false
released = false

colliding = false
