/// @description Insert description here
// You can write your code in this editor

// Draw base sprite (background)
if(sprite_exists(sprite_index)) {
	draw_self();
}

// Inherit the parent event (forground)
event_inherited();

